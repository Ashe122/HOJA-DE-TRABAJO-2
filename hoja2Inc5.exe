// Angelyn Diaz

#include <iostream>
using namespace std;

int main() {
    // Mensaje de bienvenida
    cout << "****Bienvenido Personita****" << endl;

    // Aquí se declara la variable
    int calificacion;

    // Solicitar al usuario que ingrese el resultado de su examen
    cout << "Por favor, ingresa el resultado de tu examen: " << endl;
    cin >> calificacion;

    // Aquí se comprueba si el estudiante aprobó o no el examen
    if (calificacion >= 60) {
        // Mensaje si el estudiante aprobó el examen
        cout << "¡Muy bien! Has aprobado el examen con " << calificacion << " puntos." << endl;
    } else {
        // Mensaje si el estudiante no aprobó el examen
        cout << "Lo siento. No has aprobado el examen." << endl;

        // Mensaje de ánimo para la próxima vez
        cout << "---------suerte para la próxima---------" << endl;
    }

    return 0; // Indica que el programa se ejecutó correctamente
}
